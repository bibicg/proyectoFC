<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Botón solo icono, sin fondo -->
    <style name="ButtonIconOnly" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:padding">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Botón completo amarillo con texto -->
    <style name="ButtonYellowFull" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/yellow_cartaller</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:drawablePadding">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!--fragmen job detail-->
    <style name="LabelTitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">#666666</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="LabelValue">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#000000</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">#DDDDDD</item>
        <item name="android:layout_marginBottom">12dp</item>
    </style>


</resources>
